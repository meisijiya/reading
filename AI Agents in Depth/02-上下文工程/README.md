# 第二章 上下文工程

> **全书最关键章**。一个中等能力的模型配上精心组织的上下文，往往能胜过一个顶级模型在信息匮乏下的盲目摸索。

## Q2-1 上下文工程为什么决定 Agent 能力上限？上下文工程的本质是什么？

- 章节: ch003 §2.1
- 主题: 上下文 = 能力上限

> 📖 **原文** (ch003 §2.1)：这三类信息——代码信息、流程规范、环境信息——构成了 Agent 有效工作的最低信息需求。这里进入上下文的是对环境的观察、描述或配置，而不是环境本身；Environment 仍是 Agent 在外部与之交互的对象。模型本身的智力只是基础， **上下文的质量才是 Agent 能力的真正关键** 。一个中等能力的模型配上精心组织的上下文，往往能胜过一个顶级模型在信息匮乏下的盲目摸索。

> 📖 **原文** (ch003 §2.1)：上下文工程不仅仅是一个 **技术问题** ，更是一个 **组织问题** 。大多数团队的关键知识都是隐性的……如果团队本身就是一个信息黑洞，再好的 AI Agent 也无计可施。 **对远程工作友好的团队往往也对 AI Agent 友好** 。

> 📖 **原文** (ch003 §2.1)——引用 OpenAI 翁家翌： "人和模型一样，最重要的是 Context。" …… **AI 短时间内无法取代人的最大原因也是 context** ——因为 AI 跟人并不在同一个环境里面。

🧭 **归纳**：
- 上下文工程不只是塞信息，而是系统性地设计、组织、提供完成任务所需的背景知识
- 模型智力是基础，上下文质量才是上限
- **远程友好型团队 ≈ AI 友好型团队**——信息透明、可检索、结构化的组织文化是上下文工程的组织前提
- 决定 Agent 价值的天花板往往不是模型参数，而是"每个决策点能获得多少、多精准的上下文"

## Q2-2 大模型 API 的请求结构是什么？四种消息角色 + tools 字段

- 章节: ch003 §2.2.1
- 主题: API 消息结构

> 📖 **原文** (ch003 §2.2.1)：大模型 API 的核心是一个 **消息列表（messages）** ，列表中的每条消息都有一个 **角色（role）** 标识 …… system …… user …… assistant …… tool …… 此外，工具定义（tools）作为请求的独立字段（而非消息），告诉模型有哪些工具可以使用 …… "四种消息角色 + tools 字段" 恰好覆盖第一章所说的五个上下文组成部分。

🧭 **归纳**：

| 角色 | 来源 | 对应第一章五组件 |
|---|---|---|
| `system` | 开发者 | 系统提示词 |
| `user` | 终端用户（含 RAG 注入） | 用户消息 |
| `assistant` | 模型自身 | 模型回复 |
| `tool` | Agent 框架执行后 | 工具执行结果 |
| 顶层 `tools` 字段 | 开发者 | 工具定义 |

➕ **补充**：tool 消息必须用 `tool_call_id` 与 assistant 消息中的 `tool_calls` 关联；混用 user 消息假装 tool 结果会破坏 Chat Template 的多步思考（详见 Q2-5）。

## Q2-3 ReAct 循环的 API 实现：Agent 框架的核心工作就是管 messages 列表

- 章节: ch003 §2.2.3-2.2.5
- 主题: Agent 核心循环的代码形态

> 📖 **原文** (ch003 §2.2.3)： 模型并没有直接回答用户的问题，而是返回了两个 **工具调用请求** ——它判断"当前时间"和"天气"需要通过工具获取，而且两者之间没有依赖关系，可以并行调用。 **模型只是发出了调用请求，真正执行工具的是 Agent 框架** 。这是理解 Agent 架构的关键：模型负责决策（调用什么工具、传什么参数），Agent 框架负责执行（实际调用 API、运行代码）。如果用户认为还需要更多信息（比如追问"那东京呢？"），Agent 框架会把用户的追问追加到对话历史的末尾，然后发起又一次模型 API 调用。模型会再次开始返回 tool_calls，Agent 框架再执行、再送回结果，如此循环。

> 📖 **原文** (ch003 §2.2.5)： 从这个过程可以清楚地看到： **Agent 框架的核心工作就是管理这个 messages 列表** ——在合适的时机往里追加消息，然后把整个列表送给模型。本章后续所有的上下文工程技术，本质上都是在优化这个列表的内容和结构。

🧭 **归纳**：Agent 框架的最小骨架 = while 循环 + messages 列表追加。生产代码需加 `max_iterations` 上限（Agent 可能反复调同一工具死循环）。理解 Agent 行为的唯一可靠方式：看完整 messages 列表的演化，不只看单次模型响应。

➕ **补充**：当两个工具调用无依赖时可并行（同一 assistant 消息发出多个 tool_calls）；有依赖时只能串行（后续轮的 tool_calls）。

## Q2-4 KV Cache 的三条核心结论是什么？

- 章节: ch003 §2.3 + 技术门槛提示
- 主题: KV Cache 友好的上下文设计

> 📖 **原文** (ch003 §2.3 技术门槛提示)： 如果你不熟悉这些底层机制， 可以跳过原理细节，只需记住以下三条核心结论 ： **系统提示词和工具定义一旦确定就不要改。** 任何改动，哪怕多一个空格，都可能改变 token 序列，使首个不同 token 及其后的缓存无法复用；改动越靠前，延迟和成本影响通常越大 …… **动态信息永远追加到末尾** ——时间戳、用户状态等变化的内容，作为新消息追加到对话末尾，而不是修改已有的系统提示词。 **使用标准 API 格式，不要自行拼接消息** ：结构化消息会被 Chat Template 翻译成模型训练时见过的固定 token 序列；自行用字符串拼成 "USER: ... ASSISTANT: ..." 的根本问题是偏离了这种训练格式，会削弱模型的多步思考能力。

🧭 **归纳**：KV Cache 三条铁律——
1. **静态不动**：system prompt + 工具定义定下来就别改
2. **动态追加**：时间戳、状态等变化内容追加到末尾
3. **标准格式**：用 API 的结构化消息，不要手拼字符串

➕ **补充**：反面教材——某团队客服 Agent 在 system 里加 `Current time: {{now}}` 当日实时注入，导致每月 10 万次对话的 KV Cache 命中率崩溃，首 token 延迟 0.5s → 3-5s，账单翻倍。

## Q2-5 什么是 Chat Template？为什么必须用标准 API 消息格式？

- 章节: ch003 §2.3.1
- 主题: Chat Template

> 📖 **原文** (ch003 §2.3.1)： **Chat Template 是一块贯穿全书的地基** ：它不只关系到 KV Cache，还决定了多轮工具调用、思维链保留、状态栏注入等诸多机制能否正确工作 …… 可以把 Chat Template 想象成 **信封格式** ：API 消息是信的内容，Chat Template 规定了如何在信封上写明寄件人、收件人——用特殊标记（如 `<|im_start|>system`、`<|im_end|>` ）划分每条消息的边界和角色。不同的模型家族（Qwen、Llama、Gemma）使用不同的"信封格式" …… **对于 Agent 开发者来说，你不需要手动编写或修改 Chat Template** ——API 服务端会自动处理。

> 📖 **原文** (ch003 §2.3.1)：以 Qwen3 的 Chat Template 为例：模型在多轮工具调用中，会把之前的内部思考过程（ `<think>` 标签内的内容）保留下来 …… 但当 Chat Template 检测到新的用户查询时，会默认"用户换了个话题"，于是清理之前的思考过程重新开始。问题在于，如果工具结果被错误地标记为用户消息，就会误触发这种清理 …… 严重 **影响多步思考的连贯性** 。

🧭 **归纳**：
- Chat Template = 模型训练时学会的"信封格式"，API 消息（JSON）→ token 流（线性）由它转换
- 工具结果错标 user 消息 → 误触发"换话题"清空 → 思考过程丢失 → 多步推理失败
- 不同模型家族对历史思维链策略差异大且快速变化（DeepSeek R1 剥离 → V4 强制回传；Claude 需带签名 thinking block 回传），用前查最新文档

## Q2-6 提示工程的关键设计原则是什么？

- 章节: ch003 §2.4 + 实验 2-4
- 主题: 提示工程

> 📖 **原文** (ch003 §2.4)： 提示工程（Prompt Engineering）的核心对象是 **系统提示词（System Prompt）** ——API 消息列表中那条 role: "system" 的消息。它是 Agent 的"员工手册"，定义了 Agent 的身份、行为规则、约束条件和工作流程。一个精心设计的系统提示词，能让模型在具体任务中充分发挥其通用能力。

> 📖 **原文** (ch003 §2.4 实验 2-4)： **维度一：语气与风格** ——我们实现了三种截然不同的风格。默认保持专业中立的商务语气；Trump 风格使用夸张修辞和极度自信的表达（"我会给您订到史上最棒的航班，没人比我更会订票"）；Casual 风格则采用轻松的口吻和大量表情符号。**虽然风格显著改变了表达方式，但对任务完成率的影响相对有限，说明模型具有强大的风格适应能力。** **维度二：信息组织** ——保留所有规则的内容但打乱组织结构，去除标题层次，把有序的流程拆散成无序的规则集合。这个看似简单的改变带来了灾难性的后果：任务成功率下降超过 30%，Agent 经常违反关键的业务规则 …… **对人类友好的信息组织方式，对模型同样友好。** **维度三：工具描述** ——保留函数签名和参数定义，但移除所有的描述性文本。结果工具调用的错误率增加了 45%，Agent 频繁地传递无效的参数值、错误理解参数的含义。

🧭 **归纳**（提示工程四要素）：

| 要素 | 实验结论 |
|---|---|
| 语气与风格 | 影响有限（模型风格适应力强） |
| **信息组织** | 极其关键——打散层次 → 成功率 ↓ 30%+ |
| **工具描述** | 极其关键——删描述 → 错误率 ↑ 45% |
| Few-shot 示例 | 何时给参考样例（详见 §2.4.5） |

➕ **补充**：业务规则应写成"作用域 + 动作 + 例外 + 验证方式"四元组，不要堆"禁用词表"。

## Q2-7 提示注入（Prompt Injection）是什么？怎么防御？

- 章节: ch003 §2.4.7 + 实验 2-5
- 主题: 提示注入攻防

> 📖 **原文** (ch003 §2.4.7)： 精心设计的提示工程能让 Agent 遵循复杂的业务规则，但如果攻击者能够向 Agent 的上下文中注入恶意指令，所有的规则都可能被绕过。 **提示注入（Prompt Injection）** 是 Agent 安全的核心威胁之一。其本质是：攻击者通过 Agent 处理的外部内容（网页、邮件、文档等），将伪装成系统指令的文本混入上下文，从而劫持 Agent 的行为。

> 📖 **原文** (ch003 §2.4.7)： 在上下文层面，防御的核心是帮模型分清"指令"与"数据"： **来源标记** ：用 `<external_content source="webpage">…</external_content>` 明确来源 …… **结构化角色** ：严格利用 Chat Template 的角色体系 …… **输入清洗** ：过滤外部内容中的可疑模式 …… 这层防御容易被措辞变体绕过，只能作为辅助手段。

🧭 **归纳**（分层防御）：

| 层级 | 防御手段 | 备注 |
|---|---|---|
| 上下文层 | 来源标记 / 结构化角色 / 输入清洗 | 第一道防线，**不能做到万无一失** |
| 执行层 | 权限控制 / 沙箱隔离 / 高风险独立审查 | 第四、五章展开 |
| 检索层 | RAG 投毒防护 | 第三章展开 |

➕ **补充**：Skill 机制本身是新的注入面（"把外部内容当指令加载"的制度化形式）——安装第三方 Skill 前必须审查其内容，如同审查要执行的代码。状态栏同理（模型几乎无条件信任状态栏——一旦写错，错误原样传进最终答案）。

## Q2-8 Agent Skills 的三层渐进式披露是什么？怎么写一份可用 Skill？

- 章节: ch003 §2.5
- 主题: Agent Skills

> 📖 **原文** (ch003 §2.5.1)： Agent Skills 的核心思想是将 Agent 的能力模块化为独立的、可按需加载的知识包 …… 采用了 **渐进式披露（Progressive Disclosure）** 的设计哲学——先给 Agent 看一份目录摘要，需要时再加载完整内容，就像你不会把公司所有部门的操作手册都堆到新员工桌上，而是先给一份总目录，需要哪本再去取。

🧭 **归纳**（Skills 三层结构）：

| 层 | 内容 | 时机 | 上下文成本 |
|---|---|---|---|
| 第一层 | SKILL.md 头部 YAML 元数据（name + description） | 常驻可见 | 极小（每条 ~100 token） |
| 第二层 | 完整 SKILL.md 正文 | Skill 被路由命中后加载 | 中等（数 KB） |
| 第三层 | 子文档（通过文件引用） | 进一步按需深入 | 大（按需） |

**编写 Skill 的四部分**（宝玉《图解 Skill》）：
- **角色与读者**：服务谁、面向什么任务、输出标准
- **核心原则**：3-5 条最重要的判断 + 正反例
- **禁止清单**：高频错误、越权动作、容易误解的表达 + 合法例外
- **参考资料**：术语表、模板、范文、详细子文档

**description 字段是路由决策的关键**——应写"何时使用"边界（路由条件）而非"我能做什么"（功能介绍）；宽泛描述 = 路由失准。

## Q2-9 Agent 状态栏的本质是什么？为什么有效？

- 章节: ch003 §2.6.1
- 主题: Agent 状态栏

> 📖 **原文** (ch003 §2.6.1)： Agent 状态栏之所以有效，源于注意力机制的一个本质特性：上下文学习更像检索而非推理——模型擅长从已有内容中查找信息，但不擅长主动归纳和总结 …… **上下文窗口是一台只有一半的检索引擎** …… 它"检索"的这一半非常强……但它缺了另一半： **没有"提炼层"** 。上下文里的东西从来不会被自动数一遍、建个索引、或就地总结成一条结论；任何"关于这些内容的结论"——一共多少条、有没有超标、进展到哪一步——模型每次要用，都得从原始记录里现算一遍。

> 📖 **原文** (ch003 §2.6.1)——上下文蒸馏量化结论： 给模型配上一条 **提前算好的状态栏** ， **弱模型补回来的是准确率** 。最弱的几个模型准确率能涨 40 到 54 个百分点…… **强模型本来就答得对，省下来的是效率** 。同一条状态栏，让每次查询的思考量、延迟、花费各降大约一个数量级（思考 token 砍掉八九成以上）…… **不带状态栏时，每次查询的思考量随上下文变长而持续增长；带上状态栏后，它变得基本恒定** 。

🧭 **归纳**：
- 状态栏 = 把分散在上下文各处的 **隐式状态提炼为显式知识** = 给"只有一半"的检索引擎补"提炼层"
- 效果 = 弱模型涨准确率、强模型省效率
- 状态栏追加在上下文末尾 → 临近模型新 token → 强制注意力引导
- **三条经验**：
  1. 状态栏用代码维护，**别拿大模型去维护**（一次批量统计 = 把难题搬家）
  2. **不要删掉原始上下文**——状态栏是"有损投影"，留它可省 token，丢原始记录遇未算维度会断崖
  3. **把状态栏准确率当一线生产指标盯**——模型几乎无条件信任，写错原样传进最终答案

## Q2-10 状态栏的两种实现：替换 vs 持久追加，怎么选？

- 章节: ch003 §2.6.4
- 主题: 状态栏实现取舍

> 📖 **原文** (ch003 §2.6.4)： "追加不破坏缓存" 只在单次注入时成立。状态是会变的——下一轮 TODO 完成了一项、工具计数加了一次，状态消息就过时了。**如何更新它**，存在两种实现，各有明确的缓存代价：

🧭 **归纳**：

| 实现 | 做法 | KV Cache 代价 | 适用 |
|---|---|---|---|
| **每轮替换** | 调 API 前移除旧状态、末尾追加新状态 | 旧状态位置之后的新增消息缓存失效（短后缀） | 状态大、更新频繁、轨迹长 |
| **持久追加** | 状态消息永久保留，每轮只追加新状态（Claude Code `<system-reminder>`） | 缓存完全友好 | 状态小、两次更新间新增消息多、会话长度受控 |

**粗略分界点**：当 `α·S·N/2 < (1-α)·R` 时倾向持久追加；否则替换。
- α = 缓存输入单价相对普通输入的折扣
- S = 单条状态 token 数
- R = 两次更新间新增后缀 token 数
- N = 预计更新次数

## Q2-11 上下文压缩的两个动机是什么？为什么"装得下但找不到"比"装不下"更可怕？

- 章节: ch003 §2.7.1-2.7.2
- 主题: 压缩的动机 + 上下文腐化

> 📖 **原文** (ch003 §2.7.1)： 压缩上下文有两个截然不同的动机，理解这一点对设计压缩策略至关重要。第一，**解决长度约束和成本约束** …… 第二，**提升思考质量——总结后的知识比原始形式更利于模型使用** 。这个动机更深层，也更容易被忽视。即使上下文窗口足够大，把所有原始信息堆在上下文里也不是最优选择。

> 📖 **原文** (ch003 §2.7.2)： 此外，长上下文会导致检索精度的下降。明明上下文窗口还远没有满，但 Agent 突然找不到关键信息了，或者反复纠结于一个早已解决的问题，这种现象被称为 **上下文腐化（Context Rot）** 。 上下文腐化与上下文溢出（窗口用完）是不同的问题： **溢出是"装不下了"，腐化是"装得下但找不到了"** ——后者更隐蔽，因为 Agent 表面上还在正常工作，只是决策质量悄然下降。

🧭 **归纳**：
- **压缩动机 1**：长度/成本约束——直观
- **压缩动机 2**：思考质量——把需要"思考才能得到的结论"变成"可以直接检索的知识"
- **上下文腐化 vs 溢出**：
  - 溢出 = 装不下（窗口满）→ 任务直接失败
  - 腐化 = 装得下但找不到（注意力被分散）→ 决策质量悄然下降，**更隐蔽**

## Q2-12 压缩与 KV Cache 看似矛盾实则互补

- 章节: ch003 §2.7.3
- 主题: 压缩时机

> 📖 **原文** (ch003 §2.7.3)： 关键在于理解压缩发生的 **时机和位置** 。压缩不是在单次 API 调用的过程中修改上下文，而是在 **两次 API 调用之间** ，由 Agent 框架对消息列表进行预处理：
> 1. System Prompt 和 Tool Definitions 永远不动 ——这是上下文最前面的"静态前缀"，KV Cache 持续缓存。
> 2. 压缩的对象是对话历史中的 tool results ——当 Agent 框架用压缩后的摘要替换原始的工具输出时，替换位置之后的缓存会失效，但之前的缓存仍然有效。
> 3. 这是一个有意识的权衡 …… 压缩的频次需要权衡——频繁压缩会频繁破坏缓存，最好在上下文接近阈值时批量压缩，而不是每轮都压。

🧭 **归纳**：压缩 = 在两次 API 调用之间替换 tool results 摘要。静态前缀永不动，破坏的只是替换位置之后的"短后缀"。**批量压缩优于每轮压缩**——频次过高会反复击穿缓存。

## Q2-13 什么时候应该用子 Agent 隔离而不是压缩？

- 章节: ch003 §2.7.7
- 主题: 隔离优于压缩

> 📖 **原文** (ch003 §2.7.7)： 压缩是在信息已经进入上下文之后做减法，而一个更釜底抽薪的思路是：**让大体积的中间信息根本不进入主上下文**。这就是 **子 Agent 上下文隔离** ——主 Agent 把"在代码库中大范围搜索"这类会产生海量中间内容的任务，委派给一个独立的子 Agent；子 Agent 在自己的上下文中完成探索，只把几百 token 的结论性摘要回传给主 Agent。这本质上是 **用隔离代替压缩** ：压缩是有损的、需要额外 LLM 调用的事后补救；隔离则让噪声从一开始就与主上下文绝缘，主 Agent 的 KV Cache 前缀也完全不受影响。

🧭 **归纳**：**隔离优于压缩**——独立子任务用子 Agent 跑在自有短上下文里，主 Agent 只收压缩后的结果。**把"上下文膨胀"问题转化为"上下文分发"问题**。
